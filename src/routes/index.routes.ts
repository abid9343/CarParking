// const airPort = require("./airPort.routes");
// const express = require('express');
// const app = express();
// const ROUTERS = [airPort];

// const registerRouters = (app: any) => ROUTERS.map((router) => app.use(router));

// module.exports = registerRouters;
